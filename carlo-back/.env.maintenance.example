# Private maintenance process only; never load this file in the API or frontend.
# Copy the exact Direct/Session URL from Connect. Use schema=acutis,
# sslmode=require&sslaccept=strict and the verified CA when required by the endpoint.
DIRECT_URL=
